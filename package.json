{
  "name": "vintallife-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "start": "node server/index.js"
  }
}
